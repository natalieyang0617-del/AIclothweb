import PreorderDashboard from "@/components/PreorderDashboard";

export const metadata = {
  title: "Production Queue — Vibe",
  description:
    "View your preordered pieces and secure priority launch notification.",
};

export default function DashboardPage() {
  return <PreorderDashboard />;
}
